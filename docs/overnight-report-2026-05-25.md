# 微作 / manage-your-cli — 通宵 QA + 端到端迭代报告

_2026-05-24 夜 → 05-25 晨 · 7×24 经理模式 · 自主执行_

## 0. 这一夜做了什么

1. 搭了**真机同款 QA 流水线**:Playwright 渲染 APK 静态导出(`apps/mobile/out/`)→ 回环到真桌面 `:3110` 取真实数据 → 截图。给无头 chromium 装了微软雅黑(`~/.local/share/fonts/msyh.ttc`)解决中文豆腐块。
2. 跑了 **3 个并行专项审查 agent**(Sonnet,省 token):产品/人设、UX/设计、QA 对抗测试。
3. **落地了一批修复并验证**(见 §5)。
4. 发现 **3006 预览的 `.next` 腐坏**(缺 chunk→500)——不影响手机 APK,但建议重启那个 dev server。

> ⚠️ 教训:`/tmp` 会被清(本报告原写在 /tmp 丢了,已改写到仓库)。长时后台 agent 在本环境**不可靠**(2 个跑 ~2.8h 后 socket 断),关键件改由我直接做。

---

## 1. QA 对抗测试 —— 13 个 bug(无崩溃/无 500,错误路径都 4xx)

**P1 数据完整性/安全(已全部修复 ✓):**
- BUG-001 PATCH 不校验类型(name 可存数字 → 手机 `name.slice()` 崩)。
- BUG-002 PATCH `status` 能绕过 DELETE 生命周期 → 残留孤儿 tmux 进程。
- BUG-003 `system_prompt` 无上限(100KB 也存,每轮注入 → 撑爆 token)。
- BUG-008 `max_concurrent_jobs` 可存 0/-1。

**P2 行为错误:**
- BUG-006 ✓ cli/output 对不存在员工返回 no_session(应 404)。
- BUG-007 ✓ 并发聊天 502(应 409)。
- BUG-009 ✓ lines=abc 静默回退(应 400)。
- BUG-012a ✓ todos 接受 100KB(已限 2000)。
- BUG-004 ⏳ PATCH 返回裸对象 vs GET `{staff}`(涉及改手机端,留白天)。
- BUG-005 ⏳ 详情页尾斜杠 308 会丢 device token 头(需查 Capacitor 行为)。

**P3 小问题:** BUG-010 addTodo 吞服务端错误信息、BUG-011 polish 文档不一致(留白天)。

---

## 2. UX / 设计美学 —— 8 项

**已修 ✓:** P0-1 导航 emoji→SVG 图标;P0-2 名字重复;P1-4 我tab 人设去重。
**留白天(polish):** P0-3 三种绿统一成品牌绿;P1-2 员工表单包卡片;P1-3 看板空态做成卡片;P1-1 发消息 CTA 提到 hero;P1-6 收件人切换器并入标题栏(省 54px)。

---

## 3. 产品/人设 —— 5 个能力缺口(需你定优先级)

| # | 缺口 | 影响 | 工作量 |
|---|------|------|--------|
| 1 | 交付物没有"接受/拒绝/批注" | 人审闭环合不上 | S(需确认数据层 PATCH 是否已有) |
| 2 | 看板任务卡硬编码"排队中",无实时 | 进行中 tab 信息死 | M(复用 /cli/output) |
| 3 | 无服务端推送(只能轮询) | 不会主动冒泡 = 工具非秘书 | M(SSE) |
| 4 | 移动端不能新建员工 | 出门无法临时拉人 | M |
| 5 | 交付物正文 raw `<pre>` 不渲染 markdown | 长报告不可读 | S |

最高商业密度 persona = **销售总监(外联/BD)**,微信依赖重 → 与"WeChat clawbot 当主入口"一致。

---

## 4. 待你拍板(早上)

1. **harvest-on-retire**(任务 #15):员工销毁前回收记忆进 boss —— 你说再想想。
2. **产品缺口 #1–#5 优先级** —— 我建议先做 **#5(markdown,S)+ #2(看板实时,M)+ #1(交付接受/拒绝,S)** 闭合"派活→监控→验收"主循环。
3. **跨网**:中国试点 = WeChat clawbot 打底 + gnway 穿透补全功能;Tailscale 出局(墙)。
4. **重活是否转 Codex**(今晚为稳用了 Sonnet;长时后台 agent 不稳,建议短任务化)。

---

## 5. 本夜已落地(全部 typecheck 通过)

| commit | 内容 |
|--------|------|
| `d38d569` | 名字/角色重复(通讯录+hero) |
| `963e95e` | 后端安全:PATCH 字段校验、移除 status 绕过、chat 并发→409 |
| `b0d850d` | cli/output 404/400/409、todos 文本≤2000 |
| `(导航/人设)` | 导航 SVG 图标、我tab 人设去重(出 APK 中) |

完整 persona/design/QA 三份原始报告原在 /tmp 已随清理丢失,要点已并入本报告。
